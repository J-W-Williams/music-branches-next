services:
  backend:
    build:
      context: ./server
    ports:
      - "8000:8000"
    networks:
      - merndocker

  frontend:
    build:
      context: ./client
    ports:
      - "5173:80"
    networks:
      - merndocker

networks:
  merndocker:
    driver: bridge
